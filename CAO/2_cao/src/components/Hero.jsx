import fma from "../assets/fma.jpg";
import NavBar from "./NavBar";

const Hero = () => {
  // const { name } = props;
  return (
    <div>
      <img src={fma} alt="fma" />
    </div>
  );
};

export default Hero;
